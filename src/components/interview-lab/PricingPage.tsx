// Interview Lab is free — no pricing page needed.
// This stub is kept so existing imports continue to compile.

export function PricingPage({ onUpgradeSuccess }: { onUpgradeSuccess?: () => void }) {
  void onUpgradeSuccess;
  return null;
}

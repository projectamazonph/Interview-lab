// Interview Lab is free — no subscription banner needed.
// This stub is kept so existing imports continue to compile.

export function SubscriptionBanner({ tier, onUpgrade }: { tier: string; onUpgrade: () => void }) {
  void tier; void onUpgrade;
  return null;
}
